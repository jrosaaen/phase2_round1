<div class="container">
  <h1>Welcome to DBC List!</h1>
  <p>A community exchange for toasters, tires and more!</p>
  <p>If a category isn't listed here, why don't you <a href = "/create_categories">create one?</a></p>

  <ul><% Category.all.each do |category| %>

    <li><a href="/categories/<%= category.id %>"><%= category.title %></a></li>
    <% end %>
  </ul>

<!--   <table>
    <tr>
      <th>File Description</th>
      <th>File Location</th>
    </tr>
    <tr>
      <td>This view file</td>
      <td><code><%= __FILE__.gsub(Sinatra::Application.root, '') %></code></td>
    </tr>
    <tr>
      <td>The layout file</td>
      <td><code><%= File.expand_path('../layout.erb', __FILE__).gsub(Sinatra::Application.root, '') %></code></td>
    </tr>
    <tr>
      <td>The current controller</td>
      <td><code><%= File.expand_path('../../controllers/index.rb', __FILE__).gsub(Sinatra::Application.root, '') %></code></td>
    </tr>
  </table> -->
</div>
