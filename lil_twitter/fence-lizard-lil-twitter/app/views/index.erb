<div class="container">
  <h1>Lil Twitter!</h1>
  <p>Welcome to lil-twitter. Our private twitter-clone that may or may not work.

  <br><br>Login or create an account to get started.</p>


  <form action="/user/login" method='get'>
    <button>Login</button>
  </form>
  <br>
  <form action="/user/create" method='get'>
    <button>Create Account</button>
  </form>
</div>

<div class="container">
  <h2>Updates from the Twitterverse:</h2>
  <% @tweets.sort_by{|t| t.created_at}.reverse!.each do |tweet| %>
    <h2> User: <a href=<%= "/user/#{User.find(tweet.user_id).id}/profile" %>> <%= "#{User.find(tweet.user_id).username}" %></a> </h2>
    <p><%= tweet.created_at %></p>
    <p><%= tweet.content %></p>
    <% end %>
</div>
